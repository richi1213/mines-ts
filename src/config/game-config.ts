export enum GAME_CONFIG {
  BACKGROUND_COLOR = '#0284c7',
}

export enum GAME_PHASE {
  IDLE = 'idle',
  BETTING = 'betting',
  PLAYING = 'playing',
  GAME_OVER = 'game-over',
  CASHED_OUT = 'cashed-out',
}
